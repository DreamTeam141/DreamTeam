@model Faculty.Models.CourseListViewModel

@{
    ViewBag.Title = "Available courses";
}

<h2 class="text-center">Available courses</h2>
<hr />
<p>
    @Html.Partial("_SortCoursePanel", Model.PanelViewModel)
</p>
<br />
@foreach (var item in Model.Courses)
{
    <div class="well">
        @*<img src="@Url.Action("CoursePhoto", "Course", new {id = item.Id})" class="img-rounded img-responsive media-object" width="160" height="160" />

            <h3>@Html.ActionLink(item.Title, "Details", new {id = item.Id}, new {@class = "courseTitleLink color333"})</h3>
            <p class="color333">@item.Description</p>
            <p class="color333">Duration: @item.Durations</p>
                <div class="text-right">
                    <div class="btn-group">
                        @Html.ActionLink("Details", "Details", new {id = item.Id}, new {@class = "btn btn-primary"})
                        @if (!item.IsSubscribed)
                        {
                            @Html.ActionLink("Subscribe", "Subscribe", new {id = item.Id}, new {@class = "btn btn-primary"})

                        }
                    </div>
                </div>
            </div>*@
        <div class="media">
            <div class="media-left media-middle">
                <img src="@Url.Action("CoursePhoto", "Course", new {id = item.Id})" class="media-object coursesStudIndex" />
            </div>
            <div class="media-body">
                <h3>@Html.ActionLink(item.Title, "Details", new { id = item.Id }, new { @class = "courseTitleLink color333" })</h3>
                <p class="color333">@item.Description</p>
                <p class="color333">Duration: @item.Durations</p>
                <div class="text-right">
                    <div class="btn-group">
                        @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-primary" })
                        @if (!item.IsSubscribed)
                        {
                            @Html.ActionLink("Subscribe", "Subscribe", new { id = item.Id }, new { @class = "btn btn-primary" })

                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

}
@*
    <table class="table table-hover table-striped">
        <tr>
            <th class="col-md-2 text-center">
                @Html.DisplayNameFor(model => model.Course.Title)
            </th>
            <th class="col-md-6 text-center">
                @Html.DisplayNameFor(model => model.Course.Description)
            </th>
            <th class="col-md-1 text-center">
                @Html.DisplayNameFor(model => model.Course.Durations)
            </th>
            <th class="col-md-3 text-center"></th>
        </tr>

        @foreach (var item in Model.Courses)
        {
            <tr class="text-justify">
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Durations)
                </td>
                <td class="text-right">
                    <div class="btn-group">
                        @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-primary" })
                        @if (!item.IsSubscribed)
                        {
                            @Html.ActionLink("Subscribe", "Subscribe", new { id = item.Id }, new { @class = "btn btn-primary" })

                        }
                    </div>
                </td>
            </tr>
        }

    </table>

*@
